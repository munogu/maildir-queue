{
  "name": "@munogu/maildir-queue",
  "version": "0.1.0",
  "description": "File system backed lock-free and atomic message queue for Node.js",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "NODE_ENV=test mocha --recursive 'test/**/*.spec.js'",
    "coverage": "NODE_ENV=test istanbul cover _mocha -- --recursive"
  },
  "author": "Eymen Gunay <eymen@egunay.com>",
  "license": "MIT",
  "dependencies": {
    "debug": "^3.1.0",
    "fs-extra": "^5.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.0",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}
