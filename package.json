{
  "name": "maildir-queue",
  "version": "0.1.0",
  "description": "File system backed lock-free and atomic message queue for Node.js",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "NODE_ENV=test mocha --recursive --reporter mocha-circleci-reporter 'test/**/*.spec.js'",
    "coverage": "NODE_ENV=test istanbul cover _mocha -- --recursive --reporter mocha-circleci-reporter"
  },
  "author": "Eymen Gunay <eymen@egunay.com>",
  "license": "MIT",
  "dependencies": {
    "async": "^2.6.0",
    "debug": "^3.1.0",
    "fs-extra": "^5.0.0",
    "shortid": "^2.2.8"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.0",
    "mocha-circleci-reporter": "0.0.2",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}
